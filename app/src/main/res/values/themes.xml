<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.QuranPlayer" parent="Theme.MaterialComponents.DayNight.DarkActionBar">


        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blueColor</item>
        <item name="colorOnPrimary">@color/whiteColor</item>


        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>


    <style name="AppFullScreenTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- style of reader container -->
    <style name="ReaderContainer">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/item_reader_bg</item>
        <item name="android:layout_marginStart">@dimen/largeMargin</item>
        <item name="android:layout_marginTop">@dimen/smallMargin</item>
        <item name="android:layout_marginEnd">@dimen/largeMargin</item>
        <item name="android:layout_marginBottom">@dimen/smallMargin</item>
    </style>

     <!-- style of reader name -->
    <style name="ReaderName">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">@dimen/largeMargin</item>
        <item name="android:layout_marginStart">@dimen/smallMargin</item>
        <item name="android:layout_marginEnd">@dimen/largeMargin</item>
        <item name="android:layout_marginTop">@dimen/smallMargin</item>
        <item name="android:layout_marginBottom">@dimen/smallMargin</item>

        <item name="android:fontFamily">@font/the_sans_bold</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/narvikColor</item>
        <item name="android:textSize">@dimen/readerNameFontSize</item>

    </style>

     <!-- style of reader image -->
    <style name="ReaderImage">
        <item name="android:layout_width">@dimen/largeMargin</item>
        <item name="android:layout_height">0dp</item>

        <item name="android:layout_marginStart">@dimen/largeMargin</item>
        <item name="android:layout_marginTop">@dimen/smallMargin</item>
        <item name="android:layout_marginBottom">@dimen/smallMargin</item>
        <item name="android:tint">@color/dim_gray</item>
        <item name="android:gravity">center</item>

        <item name="srcCompat">@drawable/select_reader</item>
    </style>


</resources>


